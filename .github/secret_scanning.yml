# OAuth client credentials for popular connectors are intentionally committed:
# Kowork is a desktop app (OAuth public client), so these values ship in the
# binary anyway and are not treated as secrets by their issuers.
paths-ignore:
  - "packages/app/src/data/popular-mcp.ts"
